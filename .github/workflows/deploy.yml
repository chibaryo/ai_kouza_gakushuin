- name: Deploy to GitHub Pages
  uses: peaceiris/actions-gh-pages@v3
  with:
    # GITHUB_TOKEN の代わりに personal access token を使用
    github_token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
    publish_dir: ./dist
    keep_files: false
    force_orphan: true